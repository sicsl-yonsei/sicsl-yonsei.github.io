name: Refresh Scholar citations

on:
  # Weekly, Monday 08:00 UTC. (Runs from the default branch after cutover.)
  schedule:
    - cron: "0 8 * * 1"
  workflow_dispatch:

# Needs to commit refreshed counts and deploy the rebuilt site.
permissions:
  contents: write
  pages: write
  id-token: write

# Share the "pages" concurrency group with deploy.yml so the two never deploy
# at the same time.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  refresh-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Refresh citation counts from Google Scholar (SerpAPI)
        env:
          # Add a repo secret SERPAPI_KEY and a repo variable SCHOLAR_AUTHOR_ID
          # (Settings → Secrets and variables → Actions). See SETUP.md.
          SERPAPI_KEY: ${{ secrets.SERPAPI_KEY }}
          SCHOLAR_AUTHOR_ID: ${{ vars.SCHOLAR_AUTHOR_ID }}
        # No-op (exits 0) if the key/id are missing or SerpAPI is unreachable.
        run: node scripts/refresh-citations.mjs

      - name: Commit refreshed counts (if any changed)
        id: commit
        run: |
          if [ -n "$(git status --porcelain src/content/publications)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add src/content/publications
            git commit -m "chore: refresh Google Scholar citation counts [skip ci]"
            git push
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "No citation changes — skipping rebuild/deploy."
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

      # Only rebuild + redeploy when counts actually changed. This keeps the
      # workflow a clean no-op on weeks with no changes — and, importantly, on
      # repos that haven't set SERPAPI_KEY or enabled Pages yet (so it never
      # fails just because Pages isn't configured).
      - name: Build with Astro
        if: steps.commit.outputs.changed == 'true'
        uses: withastro/action@v6
        with:
          node-version: 22

      - name: Deploy to GitHub Pages
        if: steps.commit.outputs.changed == 'true'
        id: deployment
        uses: actions/deploy-pages@v5
